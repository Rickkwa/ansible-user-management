version: '3'

services:
  centos6:
    build:
      context: ./environment
      dockerfile: centos-6.Dockerfile
    ports:
      - "2200:22"

  centos7:
    build:
      context: ./environment
      dockerfile: centos-7.Dockerfile
    ports:
      - "2201:22"
